spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: backend
  mongodb:
    uri: mongodb+srv://agrim8:agrim8@cluster0.h7iurom.mongodb.net/?appName=Cluster0
      
server:
  port: ${PORT:8080}

app:
  frontendUrl: ${FRONTEND_URL:http://localhost:5173}
  jwtSecret: ${JWT_SECRET}
  jwtExpirationMs: 86400000 # 24 hours
